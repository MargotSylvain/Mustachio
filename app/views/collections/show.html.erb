<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3">
      <p><%= @collection.movie.title %></p>
      <p><%= @collection.movie.year %></p>

      <p><% @collection.tags.each do |tag|%></p>
      <p><%= tag.name%></p>
      <%end%>
      <%= render 'review_form' %>
    </div>
  </div>
</div>
